
//Second interface
public interface Interfaces2 {
    //If both interfaces contain a same signature abstract method then class that implements these interfaces needs to provide only one implementation to fulfil the contract
    void move();
    //Cannot access the data members and static methods in another interface, even if interface extends original interface
    //Interfaces1.MYVAR;


}
